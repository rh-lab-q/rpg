class PackageBuilder:
    def build(self, spec_file, tarball):
        """builds RPM package in mock from given spec_file and tarball,
           returns None or string in case of error occurrence"""
        pass
